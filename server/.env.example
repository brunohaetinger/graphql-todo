SECRET=secret-here
#  host.containers.internal allows the container to reach the host machine's network (where Postgres is running if you use Podman or Docker with bridge networking).
DATABASE_URL=postgres://postgres:postgres@host.containers.internal:5432/tododb